name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: 🧪 Run Tests
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3
      
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        
    - name: 🧪 Run tests
      run: |
        node tests/test.js

  deploy:
    name: 🚀 Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: test  # Ждет пока тесты пройдут
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3
      
    - name: 🚀 Deploy
      run: |
        echo "✅ Тесты прошли! Деплоим на GitHub Pages..."
        echo "Сайт будет доступен по адресу:"
        echo "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
        
    # GitHub Pages автоматически деплоит при пуше в main
